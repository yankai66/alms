"""api.v1 package.

Keep this file minimal to avoid circular imports.
Submodules (users, assets, configurations, etc.) are
imported directly where needed, e.g.:

    from app.api.v1 import configurations          # standard implicit submodule import
    # or
    from app.api.v1.configurations import router
"""